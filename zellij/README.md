Issues:

- Zellij does not support native tmux-nvim navigation, see https://github.com/zellij-org/zellij/issues/967
- I cannot rename my session with a key binding? There's an action "zellij -s my-custom-name", but no way to bind
- Writing plugins only supports Rust and Go. API documentation seems complicated
